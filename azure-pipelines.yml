trigger:
- master

resources:
- repo: self

variables:
  dockerRegistryServiceConnection: 'd932d5a2-7e91-48b9-b339-918c998b3ae9'
  containerRegistry: 'freshersparkregistry.azurecr.io'
  
  # Image repositories
  celeryImageRepository: 'freshersparkcelery'
  celeryDockerfilePath: '$(Build.SourcesDirectory)/Dockerfile.celery'
  
  tag: '$(Build.BuildId)'
  vmImageName: 'ubuntu-latest'

  # Environment configuration
  environmentType: 'dev'  # Set to 'prod' for production
  keyVaultName: 'freshersparkkeyvault'
  rabbitmqHost: 'freshersparkrabbitmq.victoriousocean-8dff7969.centralindia.azurecontainerapps.io'  # Added internal hostname

stages:
- stage: Build
  displayName: Build and push Celery image
  jobs:
  - job: BuildCelery
    displayName: Build Celery Image
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build and push Celery image
      inputs:
        command: buildAndPush
        repository: $(celeryImageRepository)
        dockerfile: $(celeryDockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)
        buildContext: '$(Build.SourcesDirectory)'

- stage: Deploy
  displayName: Deploy Celery Worker
  dependsOn: Build
  jobs:
  - job: DeployCelery
    displayName: Deploy Celery Worker
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: AzureCLI@2
      displayName: 'Configure Managed Identity'
      inputs:
        azureSubscription: '8ce37efe-06df-47c4-b146-001d3ddfb1ba'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          # Verify system identity exists
          echo "Checking existing identity..."
          az containerapp identity show \
            --name freshersparkapp \
            --resource-group freshersparkresource
          
          # Get principal ID
          PRINCIPAL_ID=$(az containerapp identity show \
            --name freshersparkapp \
            --resource-group freshersparkresource \
            --query principalId -o tsv)
          
          if [ -z "$PRINCIPAL_ID" ]; then
            echo "##vso[task.logissue type=error]Failed to get principal ID"
            exit 1
          fi
          
          echo "Assigning Key Vault roles..."
          # For RBAC-enabled Key Vault
          az role assignment create \
            --assignee $PRINCIPAL_ID \
            --role "Key Vault Secrets User" \
            --scope "/subscriptions/$(az account show --query id -o tsv)/resourceGroups/freshersparkresource/providers/Microsoft.KeyVault/vaults/${{variables.keyVaultName}}"
          
          # Optional: Verify role assignment
          az role assignment list \
            --assignee $PRINCIPAL_ID \
            --scope "/subscriptions/$(az account show --query id -o tsv)/resourceGroups/freshersparkresource/providers/Microsoft.KeyVault/vaults/${{variables.keyVaultName}}" \
            --query [].roleDefinitionName

    - task: AzureCLI@2
      displayName: 'Deploy Celery Worker'
      inputs:
        azureSubscription: '8ce37efe-06df-47c4-b146-001d3ddfb1ba'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          # Get secrets from Key Vault
          RABBITMQ_USER=$(az keyvault secret show --name rabbitmq-user --vault-name ${{variables.keyVaultName}} --query value -o tsv)
          RABBITMQ_PASS=$(az keyvault secret show --name rabbitmq-pass --vault-name ${{variables.keyVaultName}} --query value -o tsv)
          
          BROKER_URL="amqp://$RABBITMQ_USER:$RABBITMQ_PASS@${{variables.rabbitmqHost}}:5672//"
          
          # Update container using YAML
          echo "Updating Celery worker configuration..."
          az containerapp show \
            --name freshersparkapp \
            --resource-group freshersparkresource \
            --output yaml > config.yaml
          
          # Add/update environment variables
          yq e -i '(.properties.template.containers[] | select(.name == "celery-worker").env) += [
            {"name": "CELERY_BROKER_URL", "value": "'"$BROKER_URL"'"},
            {"name": "CELERY_RESULT_BACKEND", "value": "rpc://"},
            {"name": "DJANGO_SETTINGS_MODULE", "value": "job_portal.settings"}
          ]' config.yaml
          
          az containerapp update \
            --name freshersparkapp \
            --resource-group freshersparkresource \
            --yaml config.yaml
          
          # Scale settings
          az containerapp update \
            --name freshersparkapp \
            --resource-group freshersparkresource \
            --min-replicas 2 \
            --max-replicas 4
          
          # Get latest revision name
          LATEST_REVISION=$(az containerapp revision list \
            --name freshersparkapp \
            --resource-group freshersparkresource \
            --query "[?properties.active].name" -o tsv | head -1)
          
          echo "Restarting revision: $LATEST_REVISION"
          
          # Restart the revision
          az containerapp revision restart \
            --name freshersparkapp \
            --resource-group freshersparkresource \
            --revision $LATEST_REVISION