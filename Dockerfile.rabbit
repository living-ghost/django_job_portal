# Use the official RabbitMQ image as the base
FROM rabbitmq:3-management

# Optional: Add any custom configuration, such as plugins or settings
# COPY custom_config.conf /etc/rabbitmq/rabbitmq.conf

# Expose RabbitMQ ports (AMQP and HTTP API)
EXPOSE 5672 15672

# Command to start RabbitMQ
CMD ["rabbitmq-server"]
